{
  "name": "logo image checked",
  "type": "track",
  "folder": "eventsPlusHub",
  "phase": "engagement",
  "destinations": ["datadog", "Snowflake", "Mixpanel"],
  "release": "v2.0",
  "multiStep": false,
  "owner": "red",
  "description": "this action tracks the users who saves the image in order to use as logo in events+",
  "trigger": "this action is triggered when user clicks on check mark button in edit state of website image section in branding section",
  "sdk": "analytics.js",
  "status": "active",
  "rules": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "context": {},
      "properties": { "type": "object", "properties": {}, "required": [] },
      "traits": {}
    }
  }
}
